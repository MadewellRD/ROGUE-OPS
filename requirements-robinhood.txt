#
# requirements-robinhood.txt
#
# Dependencies for the Robinhood Agentic Trading broker (MCP client).
# Only needed when BROKER/EQUITY_BROKER routes to Robinhood.
#
#   python -m pip install -r requirements-robinhood.txt
#

# Official Model Context Protocol Python SDK (HTTP/streamable client).
mcp>=1.2.0
